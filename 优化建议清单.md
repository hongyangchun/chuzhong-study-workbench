# 初中学习工作台 · 三维优化建议清单

> 基于当前代码实际结构（4 主 tab：今日/作业/记忆/错题 + ⚙️设置弹窗 + 6 个弹窗；记忆 tab 已分古诗/词汇/卡 3 子页；豆豆宠物 + 艾宾浩斯图在「今日」页；设置含导出/云同步/归档/年级/背诵库/重置 6 段；词汇已扩至 727 词）。
> 优先级：P0=高价值低成本先做；P1=中价值中成本；P2=增值探索。

---

## 1. 功能迭代

### P0（建议立刻做）
- **F1 词汇/古诗搜索与筛选** — 当前记忆 tab **全站无一处搜索**，727 词只能逐张翻。在每个子页加 sticky 搜索框（按单词/释义/首字母）+ 筛选（待开始 / 复习到期 / 已毕业 / 全部）。这是扩词后最大的痛点。
- **F2 修复写死日期 bug** — `index.html:1146` 的 `page-subtitle` 写死「今天是 2026年8月4日」，全站 `getTodayDateStr()` 却从未注入此节点。每天显示错误日期，极劝退。应改为 `document.getElementById('page-subtitle').innerText = '今天是 ' + getTodayDateStr() + ' · ...'`（1 行）。
- **F3 「今日复习队列」学习模式** — 背单词/古诗时默认进入「只练今天到期 + 待开始前 N 条」的编排队列，而非从第 1 张翻 727 张。加「开始今日复习」按钮，与「分离待开始」设计一脉相承。

### P1（第二阶段）
- **F4 导入错峰排期** — 导入一册后，前 10~15 条标「今天首轮」，其余 nextReview 顺延未来若干天，避免今日空白也不堆爆。
- **F5 测验/自测模式** — 单词/古诗支持「隐藏答案 → 自判对/错」而非直接「记住/忘记」，更接近真实背诵并能统计正确率。
- **F6 错题标签与归因** — 错题加「知识点标签」「错因分类（计算/概念/粗心）」，支持导出 CSV 复盘。
- **F7 周计划 / 周视图** — 目前只有「今日」。加「本周计划」（作业 due + 计划背诵量按周排布），契合初中期中/期末节奏。
- **F8 学习数据洞察** — 艾宾浩斯图已有，补齐：各册掌握率、连续打卡趋势（折线）、易错 TOP5，纯内联 SVG 即可。

### P2（增值探索）
- **F9 家长周报导出** — 一键生成「本周学习报告」（背诵量/打卡天数/错题消灭数）图片，闭环给家长。
- **F10 云同步自动化** — 现有 KV 同步需手动推/拉+同步码，可加「打开自动同步」开关（节流防抖）。
- **F11 计划模板** — 预设「期末 30 天冲刺」「古诗文默写专项」，一键生成每日任务。

---

## 2. 信息架构

### 现状问题
- **「今日」过载**：聚焦看板 + 豆豆宠物(`1169`) + 等级/连续打卡(`1190`) + 艾宾浩斯图(`1265`) 全挤在首屏，重点被稀释。
- **设置 modal 是大杂烩**：导出恢复 / 云同步 / 归档 / 年级 / 背诵库 / 危险重置 6 段混装，日常与危险操作无强隔离。
- **导航三份 DOM**：左侧 `nav-list`(`1098`)、顶栏 `top-nav`(`1132`)、底栏 `bottom-tab`(`1381`) 重复维护，改一处易漏两处。
- **缺「计划/目标」层级**：时间维度只有「今日」，无周/考试维度。

### 建议方案
- **A1 抽出「成长」独立 tab** — 把豆豆/等级/打卡/艾宾浩斯图移到新 tab「成长」，今日只留聚焦看板 + 今日复习队列入口，首屏聚焦。
- **A2 设置独立成页或分层** — 设置功能已多到不像弹窗：日常项（背诵库/年级/数据管理）留设置入口，云同步/危险重置收进二级「高级」。或直接做成 Settings tab。
- **A3 统一导航组件** — 用一份 JS nav 配置渲染桌面侧栏 + 移动底栏，新增 tab 只改一处。
- **A4 引入「计划」层级** — 在今日之上加周计划/考试倒计时，让今日成为「计划执行后的当日切片」。
- **A5 全局状态常驻** — 当前年级已显示于 header，建议加「待复习总数」常驻小条，点击跳今日。

**推荐目标结构（5 tab）**：今日(聚焦+队列) · 作业 · 记忆(古诗/词汇/卡) · 错题 · 成长(豆豆/等级/图表)；设置置于右上角独立页。

---

## 3. UI 设计

### 现状问题
- **行内 style 泛滥**：`style="font-size:12px; color:var(--text-sub);"`、`h4 style="color:var(--primary-green);"` 散落数十处，无类型规范，改主题要改几十处。
- **调色板混杂**：语义变量（primary-green/accent-orange/status-red）+ 硬编码 hex（`#E3F2FD` 等 tag 背景）共存，不一致。
- **图标用 emoji**（⚠️📚🎉）承载操作语义，低龄产品可接受但需统一；关键操作建议改内联 SVG 以便换色/缩放且离线可用。
- **三套导航视觉可能不一致**；移动端顶栏+底栏并存可能挤占空间。

### 设计方向与一致性规范
- **D1 建立设计令牌**：颜色（主/强调/危险/中性灰阶，收编所有裸 hex）、字号五级（display/title/body/small/caption，禁随意 12/13/15px）、圆角/间距/阴影统一变量。
- **D2 工具类替代行内 style**：抽 `.text-sub`/`.text-caption`/`.tag`/`.section-title`，新代码禁止行内 style（动态值除外）。
- **D3 卡片规范**：聚焦卡/诗词卡/单词卡/错题卡共用一套内边距、标题层级与操作区布局。
- **D4 导航统一**：一套组件双端适配，active 态/badge/图标风格统一；移动端仅底栏、桌面用侧栏，删冗余顶栏。
- **D5 空状态组件化**：抽统一 `<empty-state icon text action>`，取代各列表手写空文案。
- **D6 图标策略**：关键操作用内联 SVG path（可换色/缩放/离线），emoji 仅装饰。
- **D7 正反馈微交互**：完成打卡/背诵给轻量动效（豆豆点头、进度环增长）替代 `alert` 弹窗，强化「积少成多」。

### 交付前一致性检查清单
- [ ] 全站颜色仅来自 token，无裸 hex
- [ ] 字号仅来自五级尺度
- [ ] 无行内 style（动态值除外）
- [ ] 导航单组件双端
- [ ] 图标统一（SVG 优先）
- [ ] 空状态/弹窗/卡片组件化

---

## 建议执行顺序
1. **本周**：F2（日期 bug）→ F1（搜索筛选）→ F3（今日复习队列）。三项直接解决扩词后的可用性与一个硬 bug。
2. **下阶段**：A1+A3（信息架构瘦身与导航统一）+ D1~D3（设计令牌与组件化），作为一次「体验 groundwork」，让后续功能有统一底座。
3. **之后**：P1/P2 按需拣选。
